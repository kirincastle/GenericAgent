# [Global Memory Insight]
Read L2 or ls ../memory/ for L3 when needed
L0(META-SOP): memory_management_sop
L2 topics: oc2api_ports | oc2api_system | rtk | caveman | git_config | cache_hit_opt | socks5_poll | jpkbb_timezone(hkt) | probeModelStatus | model_registry | codegraph | ponytail | oc2api_proip_v3.059

L3 SOPs (type: sop) — 36 files with OKF frontmatter:
autonomous_operation | cf_workers | checklist | code_review | computer_use | const | edgetunnel_workers_pages_guide | github_contribution | gnhf | goal_hive | goal_hive_master_duty | goal_mode | incubator | lavish_skill | learn_eval | lessons_sop | ljqCtrl_sop | memory_cleanup | morphling | no_mistakes | oc2api_deploy | oc2api_system | okf_frontmatter_convention | plan | pre_task_scanner | procmem_scanner | project_mode | recovery | review | scheduled_task | subagent | supervisor | tmwebdriver | treehouse | verify | vision | web_setup

Search: `bash ../memory/sop_search.sh <keyword>` or `bash ../memory/lessons_search.sh <keyword>` or `python3 ../temp/.agents/skills/okf-frontmatter/scripts/find_docs.py --repo /home/moclaw/projects/external/genericagent find <term>`

Indexes (JSONL, agent-native):
- `memory/sops_index.jsonl` — SOP metadata (38 records)
- `handoffs/handoffs.jsonl` — Handoff registry (18 records)  
- `memory/mode_rules.jsonl` — Mode decision rules (7 records)

Handoffs: handoffs/index.md (OKF frontmatter, 14 entries) | Template: handoffs/HANDOFF_TEMPLATE.md

GA Commands: /goal /hive /conductor /morphling /supervisor /plan /incubator /recovery /autonomous /subagent /review /verify /project /checklist /scheduled-task /web-setup /choose-mode

## 行为规则（每轮激活）
**Lessons 自动匹配**：本会话内，每次用户给非闲聊请求，自动提取关键词，运行 `bash ../memory/lessons_search.sh <关键词>`，将匹配的 `rule` 注入工作记忆。不依赖任何特定模式。
- **"继续"/"继续上次"** → 立即读 Handoffs: handoffs/index.md，找到 `### Active` 下所有 handoff doc
  - 如果只有 1 条 → 自动继续任务
  - 如果有 ≥2 条 → 列出所有，问用户想继续哪个
- **报路径必给 full path** — 所有文件路径写绝对路径 `/home/moclaw/...`，不缩写
- **代码变更后 summary 必报 version + short_hash** — vX.XXX + commit-hash + deploy 状态
